{
  "id": "misskey",
  "name": "Misskey Channel",
  "description": "Misskey Streaming API channel for OpenClaw, using misskey-llm-cli (what) binary",
  "version": "0.1.0",
  "channels": ["misskey"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "cliBinary": {
        "type": "string",
        "description": "Path to the 'what' CLI binary. If omitted, searches PATH."
      },
      "mentionOnly": {
        "type": "boolean",
        "description": "If true, only process messages that mention the bot user."
      },
      "allowFrom": {
        "type": "array",
        "items": { "type": "string" },
        "description": "List of usernames allowed to interact with the bot. If empty or omitted, all users are allowed."
      },
      "adminUsers": {
        "type": "array",
        "items": { "type": "string" },
        "description": "List of usernames with admin privileges (e.g. can use admin-only commands)."
      }
    }
  },
  "uiHints": {
    "cliBinary": {
      "label": "CLI Binary Path",
      "placeholder": "/usr/local/bin/what"
    },
    "mentionOnly": {
      "label": "Mention-only mode"
    },
    "allowFrom": {
      "label": "Allowed Users",
      "placeholder": "[\"c30\", \"other_user\"]"
    },
    "adminUsers": {
      "label": "Admin Users",
      "placeholder": "[\"c30\"]"
    }
  }
}
